<%= form_for(@garbage) do |f| %>
  <% if @garbage.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @garbage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br />
  <div class="field_alt">
    <%= f.text_field :name, placeholder: 'Nome' %>
  </div>
  <br />
  <div class="field_alt">
    <%= f.text_field :image, placeholder: 'Imagem' %>
  </div>
  <br />
  <div class="actions">
    <%= f.submit "Salvar" %> ou <%= link_to 'Voltar', garbages_path, :class => 'cancel' %>
  </div>
<% end %>
