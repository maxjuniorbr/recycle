<!DOCTYPE html>
<html>
<head>
  <title>Recycle</title>
  <%= stylesheet_link_tag    "application", "jquery-ui-1.10.4.custom.min", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "jquery-ui-1.10.4.custom.min", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>	
	<div id="header">
		<h1><%= link_to "Recyle", root_path %></h1>
		<div id="user_bar">
			<% if logged_in? %>
				<%= link_to "Lista de lixo", products_path %> |
				<%= link_to "Lista de classificação de lixo", garbages_path %> |
				<%= link_to "Alterar senha", edit_user_path(current_user) %> |
				<%= link_to "Deslogar", logout_path %>
			<% else %>
				<%= link_to "Logar", login_path %> | 
				<%= link_to "Novo usuário", new_user_path %>
			<% end %>
		</div>
	</div>

	<div id="main">
		<%= content_tag(:p, notice, class: 'notice') if notice.present? %>
		<%= content_tag(:p, alert, class: 'alert') if alert.present? %>

		<%= yield %>
	</div>

	<footer>
				
	</footer>	
</body>
</html>
